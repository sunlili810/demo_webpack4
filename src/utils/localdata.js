/**
 * This module is used for mock testing
 * add new mock data if backend haven't implement the logic yet
 */

const localData = {
  'item1data': require('localData/item1.json'),
};

module.exports = localData;